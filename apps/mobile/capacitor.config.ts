import type { CapacitorConfig } from "@capacitor/cli";
const config: CapacitorConfig = { appId: "com.velaterm.mobile", appName: "VelaTerm", webDir: "dist", loggingBehavior: "none", ios: { contentInset: "never" } };
export default config;
